/*
 * TransferComplete Messages
 *
 * This contains all the text for the TransferComplete component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.TransferComplete.header',
    defaultMessage: 'Transfer completed!',
  },
});
